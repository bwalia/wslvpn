# Desktop

macOS desktop (`apps/wsl-desktop`) is a Tauri shell:

```text
wsl-desktop → wsl CLI / agent → control plane
```

The GUI does not run privileged network operations.

See [apps/wsl-desktop/README.md](../apps/wsl-desktop/README.md).
