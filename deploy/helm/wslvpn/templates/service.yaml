apiVersion: v1
kind: Service
metadata:
  name: {{ include "wslvpn.fullname" . }}
  labels:
    {{- include "wslvpn.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wslvpn.selectorLabels" . | nindent 4 }}
